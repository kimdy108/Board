export default interface boardInputProps {
  inputTitle: string,
  isDisabled: boolean,
  isRequire: boolean,
  isPassword: boolean,
  inputPlaceholder: string,
  inputType: string,
  inputHint: string
}